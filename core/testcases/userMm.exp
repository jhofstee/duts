#
# interactively modify memory and display result
#

expect "*"
send -s "mm 100000\r"
expect "?"

foreach v {0 aabbccdd 01234567} {
	send -s "$v\r"
	sleep 0.25
	expect "?"
}

send -s ".\r"
expect "=>"
unset v

send_cmd "md 100000 10" ".*"

