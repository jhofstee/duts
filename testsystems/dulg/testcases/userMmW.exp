#
# interactively modify memory and display result
#
global _context_firmware_prompt

expect "*"
send -s "mm.w 100000\r"
expect "?"

foreach v {0101 0202 4321 8765} {
	send -s "$v\r"
	sleep 0.25
	expect "?"
}

send -s ".\r"
expect $_context_firmware_prompt
unset v

_context_firmware_command "md 100000 10" ".*"

