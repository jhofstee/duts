#
# tftp the blob and make it the active one
#

set user_commands {
	"print fdt_addr_r" ".*"
	"print fdt_file" ".*"
	"tftp ${fdt_addr_r} ${fdt_file}" ".*"
	"fdt addr ${fdt_addr_r}" ".*"
}

set rv [run_cmds user_commands "firmware"]

unset user_commands
return $rv
